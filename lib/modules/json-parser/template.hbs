<html>
<head>
    <title>{{{name}}}</title>
    <meta charset="UTF-8">
    <link rel="stylesheet" type="text/css" href="css/app.css">
</head>
<body>
<h1 class='{{classType}}'>{{{name}}}
    {{#classAlias}}
        <span class='alias'>alias: {{{.}}}</span>
    {{/classAlias}}
</h1>
<div class="classHead">
    <div class="classMetaHeader">
        Related Classes <a class="button" href="javascript:void(0);" onClick="ExtL.onExpandToggleClick(this);">collapse</a>
    </div>
    <div class="classMeta">
        {{#if altNames}}
            <div class="classMetaColumn">
                <h3>Alternate Names</h3>
                <p class="list">{{{altNames}}}</p>
            </div>
        {{/if}}
        {{#if mixins}}
            <div class="classMetaColumn">
                <h3>Inherited Mixins</h3>
                <p class="list">{{{mixins}}}</p>
            </div>
        {{/if}}
        {{#if requires}}
            <div class="classMetaColumn">
                <h3>Requires</h3>
                <p class="list">{{{requires}}}</p>
            </div>
        {{/if}}
    </div>
</div>
<div id="centerContent">
    <div id="class-tree-ct">
        <div id="tree-header">
            All Classes
            <div id="hide-class-tree"></div>
        </div>
        <div id="tree" class="tree-shown"></div>
    </div>
    <div id="rightMembers">
        <div id="member-toolbars">
            <div id="member-display">
                <div class="member-access-display"><label><input id="publicCheckbox" type="checkbox" value="member-public" checked=true/>Public</label></div>
                <div class="member-access-display"><label><input id="protectedCheckbox" type="checkbox" value="member-protected"/>Protected</label></div>
                <div class="member-access-display"><label><input id="privateCheckbox" type="checkbox" value="member-private"/>Private</label></div>
            </div>
            <div id="toolbar">
                <div id="toggle-class-tree">&nbsp;</div>
                {{#members}}
                    {{#is $type "configs"}}
                        <div id="configs-nav-btn" class="toolbarButton">
                            <a href="#configs">configs</a>
                            <div id="configs-count" class="nav-btn-count"></div>
                            <div class="nav-btn-mask"></div>
                        </div>
                    {{/is}}
                    {{#is $type "properties"}}
                        <div id="properties-nav-btn" class="toolbarButton">
                            <a href="#properties">properties</a>
                            <div id="properties-count" class="nav-btn-count"></div>
                            <div class="nav-btn-mask"></div>
                        </div>
                    {{/is}}
                    {{#is $type "static-properties"}}
                        <div id="static-properties-nav-btn" class="toolbarButton">
                            <a href="#static-properties">static-properties</a>
                            <div id="static-properties-count" class="nav-btn-count"></div>
                            <div class="nav-btn-mask"></div>
                        </div>
                    {{/is}}
                    {{#is $type "methods"}}
                        <div id="methods-nav-btn" class="toolbarButton">
                            <a href="#methods">methods</a>
                            <div id="methods-count" class="nav-btn-count"></div>
                            <div class="nav-btn-mask"></div>
                        </div>
                    {{/is}}
                    {{#is $type "static-methods"}}
                        <div id="static-methods-nav-btn" class="toolbarButton">
                            <a href="#static-methods">static methods</a>
                            <div id="static-methods-count" class="nav-btn-count"></div>
                            <div class="nav-btn-mask"></div>
                        </div>
                    {{/is}}
                    {{#is $type "events"}}
                        <div id="events-nav-btn" class="toolbarButton">
                            <a href="#events">events</a>
                            <div id="events-count" class="nav-btn-count"></div>
                            <div class="nav-btn-mask"></div>
                        </div>
                    {{/is}}
                    {{#is $type "mixins"}}
                        <div id="mixins-nav-btn" class="toolbarButton">
                            <a href="#mixins">mixins</a>
                            <div id="mixins-count" class="nav-btn-count"></div>
                            <div class="nav-btn-mask"></div>
                        </div>
                    {{/is}}
                    {{#is $type "vars"}}
                        <div id="vars-nav-btn" class="toolbarButton">
                            <a href="#vars">css variables</a>
                            <div id="vars-count" class="nav-btn-count"></div>
                            <div class="nav-btn-mask"></div>
                        </div>
                    {{/is}}
                {{/members}}
                <div class="memberFilter">
                    <input placeholder="Filter members..." type="search" id="box" name="box">
                </div>
            </div>
        </div>
        <div id="member-toolbars-placeholder"></div>
        <div class="classText"><h2>Summary</h2>{{{classText}}}</div>
        <a href="#" id="back-to-top">Back to Top </a>
        <div class="members">
            {{#members}}
                <div id="{{{$type}}}-ct">
                <h2 id="{{{$type}}}" class="type">{{{$type}}}</h2>
                <a name="{{{$type}}}"></a>
                {{#items}}
                    <div id="{{#is ../$type "configs"}}cfg{{else}}{{{$type}}}{{/is}}-{{{name}}}" class="classmembers member{{#access}}-{{{.}}}{{/access}}" data-member-name="{{{name}}}" {{#accessor}} data-accessor="true"{{/accessor}}{{#inheritdoc}} data-inherited="true"{{/inheritdoc}}>
                        <h2>
                            <a href="#{{#is ../$type "configs"}}cfg{{else}}{{{$type}}}{{/is}}-{{{name}}}" class="member-name">
                                {{{name}}}

                                {{#if hasParams}}<span class="params-list">({{/if}}

                                {{#items}}
                                    {{#is $type "param"}}
                                        {{#unless @first}}, {{/unless}}
                                        {{{name}}}
                                        {{#if @last}}
                                            {{#is ../$type "event"}}, eOpts{{/is}}
                                        {{/if}}
                                    {{/is}}

                                {{/items}}

                                {{#if hasParams}}
                                    )</span>
                                {{/if}}
                            </a>
                            {{#items}}
                                {{#is $type "return"}}
                                    <span class="return-type">{{{type}}}</span>
                                {{/is}}
                            {{/items}}
                            {{#type}}
                                <span class='memberType'>{{{.}}}</span>
                            {{/type}}
                            {{#chainable}}
                                <span class='chainable'>chainable</span>
                            {{/chainable}}
                            {{#preventable}}
                                <span class='preventable'>preventable</span>
                            {{/preventable}}
                            {{#readonly}}
                                <span class='readonly'>readonly</span>
                            {{/readonly}}
                            {{#static}}
                                <span class='static'>static</span>
                            {{/static}}
                            {{#required}}
                                <span class='required'>required</span>
                            {{/required}}
                            {{#template}}
                                <span class='template'>template</span>
                            {{/template}}
                            {{#access}}
                                <span class='{{{.}}}'>{{{.}}}</span>
                            {{/access}}
                        </h2>
                        {{#text}}
                            <p>{{{.}}}</p>
                        {{/text}}
                        {{#value}}
                            <p class="defaults-to-dec">Defaults to: {{{.}}}</p>
                        {{/value}}
                        {{#since}}
                            <p class="since-dec">Available since: {{{.}}}</p>
                        {{/since}}
                        {{#items}}
                            {{#is $type "param"}}
                                {{#if @first}}
                                    <h3>Parameters</h3>
                                {{/if}}

                                <p>{{{name}}} :
                                {{#if type}}
                                    {{{type}}}
                                {{else}}
                                    Object
                                {{/if}}

                                </p>
                                <p>{{{text}}}</p>
                            {{/is}}

                            {{#is $type "property"}}
                                {{#unless @index}}
                                    <h3>Properties</h3>
                                {{/unless}}

                                <p>{{{name}}} : {{{type}}}</p>
                                <p>{{{text}}}</p>
                            {{/is}}

                            {{#is $type "return"}}
                                <h3>Returns</h3>
                                <p>{{{type}}}</p>
                                <p>{{{text}}}</p>
                            {{/is}}

                            {{#if @last}}
                                {{#is ../$type "event"}}
                                    <p>eOpts : <a href="Object.html">Object</a></p>
                                    <p>The options object passed to <a href="Ext.util.Observable.html">Ext.util.Observable.addListener</a>.</p>
                                {{/is}}
                            {{/if}}
                        {{/items}}
                        {{#deprecatedMessage}}
                            <p class="deprecated-dec">Deprecated: {{{.}}}</p>
                        {{/deprecatedMessage}}
                        {{#preventable}}
                            <p class='preventable-dec'>This action following this event is preventable. When any
                                of the listeners returns false, the action is cancelled.</p>
                        {{/preventable}}
                        {{#removedVersion}}
                            <p class='removed-dec'>This method has been REMOVED since {{{.}}}</p>
                        {{/removedVersion}}
                        {{#template}}
                            <p class='template-dec'>This is a template method. a hook into the functionality of this
                                class. Feel free to override it in child classes.</p>
                        {{/template}}
                    </div>
                {{/items}}
                </div>
            {{/members}}
        </div>
        <div class="members-mask"></div>
    </div>
</div>
</body>
<script type="text/javascript">
    var ExtL = ExtL || {};

    ExtL.treeData = {{{tree}}};
</script>
<script type="text/javascript" src="js/app.js"></script>
</html>
