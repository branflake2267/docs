{
  "name": "@sencha/docs-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "increment": "lerna publish --skip-git --skip-npm --force-publish",
    "clean": "lerna clean",
    "install:clean": "npm cache clear --force && lerna clean && npm install"
  },
  "devDependencies": {
    "lerna": "3.13.1"
  }
}
